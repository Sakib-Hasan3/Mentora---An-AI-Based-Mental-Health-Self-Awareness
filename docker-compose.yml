frontend:
  build:
    context: ./frontend
    dockerfile: Dockerfile
  container_name: mentora-frontend
  depends_on:
    - backend
  ports:
    - "3000:3000"
  environment:
    NEXT_PUBLIC_API_BASE: "http://localhost:8000"
  volumes:
    - ./frontend:/app
    - /app/node_modules
